## 1. Browse Table Version Rename

- [x] 1.1 Add `version` colDef (name="Version"), hide `cran_version`, add github_* hidden columns
- [x] 1.2 Verify browse table renders without errors

## 2. Detail View Version Card

- [x] 2.1 Update `build_version_card()` with source-agnostic labels per spec §5.4.3

## 3. Detail View Header Fallbacks

- [x] 3.1 Update `build_header_card()` with GitHub fallbacks for title, description, maintainer, license per spec §5.4.4

## 4. Final Verification

- [x] 4.1 Run full test suite to confirm no regressions
